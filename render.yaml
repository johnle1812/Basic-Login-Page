services:
  - type: web
    name: basic-login-page
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: JWT_SECRET_KEY
        sync: false
      - key: MONGODB_URI
        sync: false
